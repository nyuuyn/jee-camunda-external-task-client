<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
      Environment entries bind named string constants into java:comp/env/.
      Both values are injected via @Resource(name="...") into NoteResource
      and AddNoteHandler respectively, so the creator names are configurable
      at the application-server level without recompiling the application.

      JNDI paths resolved by @Resource(name="<entry-name>"):
        java:comp/env/creator/restApi     → "rest api"
        java:comp/env/creator/taskHandler → "task handler"

      All CDI-managed beans within this WAR deployment (including those in
      WEB-INF/lib JARs) share this java:comp/env namespace.
    -->
    <env-entry>
        <env-entry-name>creator/restApi</env-entry-name>
        <env-entry-type>java.lang.String</env-entry-type>
        <env-entry-value>rest api</env-entry-value>
    </env-entry>

    <env-entry>
        <env-entry-name>creator/taskHandler</env-entry-name>
        <env-entry-type>java.lang.String</env-entry-type>
        <env-entry-value>task handler</env-entry-value>
    </env-entry>

</web-app>
